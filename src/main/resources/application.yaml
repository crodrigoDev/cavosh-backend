spring:
  application:
    name: cavoshbackend

  config:
    import: optional:file:${ENV_FILE:.env.local}[.properties]

  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:${POSTGRES_PORT:5432}/${POSTGRES_DB}}
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          time_zone: UTC

  flyway:
    enabled: true
    locations: classpath:db/migration

  resend:
    api-key: ${RESEND_API_KEY}
    from: ${RESEND_FROM}

  app:
    jwt:
      secret: ${JWT_SECRET}
      issuer: cavosh-api
    google:
      client-id: ${GOOGLE_CLIENT_ID}

server:
  port: ${PORT:8080}
